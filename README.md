# Max iOS build mirror

Build-only snapshot of the Max iPhone client (`apps/ios`). Each push is a
fresh snapshot with no history. The unsigned IPA is produced by the workflow in
`.github/workflows`. All rights reserved.
